#!/usr/bin/env bash

# Print the local IP

ifconfig | grep -Eo 'inet (addr:)?([0-9]*\.){3}[0-9]*' | grep -Eo '([0-9]*\.){3}[0-9]*' | grep -v '127.0.0.1' | tr -d '\n'

if [ -t 1 ]; then
  # Print a new line unless piping
  echo ''
fi
