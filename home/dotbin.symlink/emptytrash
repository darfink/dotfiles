#!/usr/bin/env bash

# Empty all file trash

case "$OS" in
'osx')
  sudo rm -rfv /Volumes/*/.Trashes
  sudo rm -rfv ~/.Trash
  sudo rm -rfv /private/var/log/asl/*.asl
  ;;
'linux')
  rm -rf ~/.local/share/Trash/*;;
'cygwin')
  win7util-empty-recycle-bin;;
esac
