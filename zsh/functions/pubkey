# Copy your public key to the pasteboard
pubkey() {
  local path="$HOME/.ssh/id_rsa.pub"

  if [ ! -e "$path" ]; then
    echo 'No public key exists'
    exit 1
  else
    cat $path | pbcopy
    echo 'Public key copied to pasteboard'
  fi
}
